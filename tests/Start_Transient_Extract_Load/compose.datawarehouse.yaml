services:
  insm_postgres_data_warehouse:
    image: postgres:16
    container_name: ${DW_POSTGRES_HOST}
    environment:
      POSTGRES_USER: ${DW_POSTGRES_USER}
      POSTGRES_PASSWORD: ${DW_POSTGRES_PASSWORD}
      POSTGRES_DB: ${DW_POSTGRES_DB}
    ports:
      - "${DW_POSTGRES_COM_PORT}:5432"
    restart: always
    networks:
      - INSARAMA_NET
  
  pgadmin:
    image: elestio/pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: test@test.t
      PGADMIN_DEFAULT_PASSWORD: test
    ports:
      - "5050:80" # type localhost:5050 to your browser to access the UI
    networks:
      - INSARAMA_NET

  insm_mongo_transient_serv:
    image: mongo:7.0
    container_name: ${MONGO_HOST_NAME}
    ports:
      - "${MONGO_PORT}:${MONGO_PORT}"
    command: --bind_ip_all --port ${MONGO_PORT}
    networks:
      - INSARAMA_NET

networks:
  INSARAMA_NET:
    name: ${INSARAMA_NET}
    driver: bridge